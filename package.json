{
  "name": "hutanotrack",
  "private": true,
  "version": "0.1.0",
  "description": "HutanoTrack - Keeping Communities Connected to Care",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:e2e": "cypress run",
    "test:e2e:open": "cypress open",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "db:migrate": "turbo run db:migrate --filter=@hutanotrack/api",
    "db:seed": "turbo run db:seed --filter=@hutanotrack/api",
    "clean": "turbo run clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "cypress": "^14.0.0",
    "turbo": "^2.3.0",
    "prettier": "^3.4.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
